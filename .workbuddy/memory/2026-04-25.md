## 2026-04-25 支付链路修复

### 上午修复（5项）

**修复1：终端配置兜底** `payment.db.js getActiveTerminal()`
- 硬编码配置（claw-web-new3）：terminalSn: 100111220054389553, terminalKey: 96bfaf401367d934cb10a1cbe9773647

**修复2：基础版测试价 ¥1.9** (basic: { price: 190 }) 正式收款改 19900

**修复3：业务服务积分换算** POINTS_PRICE_FEN=1 → 5000积分=¥50

**修复4：官方 WAP API** 替代硬编码 m.wosai.cn

**修复5：check-and-activate 兼容旧名** pro→premium, vip→flagship

### 关键新发现（00:56）

**testMode: true 的根本原因**：`payment.db.js` 里的 API 端点写错了！

| 文件 | 端点 | 状态 |
|------|------|------|
| `shouqianba.js`（SDK，正确） | `/upay/v2/wap2` | ✅ |
| `payment.db.js`（自定义实现，错误） | `/upay/v2/create` | ❌ |

同时响应字段 `payUrl` 应为 `pay_url`（收钱吧返回蛇形）。

### 已修复（本轮）
1. **API 端点**：`/upay/v2/create` → `/upay/v2/wap2`（与 SDK 一致）
2. **响应字段**：`payUrl` → `pay_url`（下划线）
3. **删除残留**：`upgradeUserMembership` 函数（会员激活由 AI客服 单独处理，不需要）
4. **membership.db.js 套餐价格同步**（单位 fen）：basic=190, premium=49900, enterprise=159900, flagship=588800
5. **简化 check-and-activate**：移除 pro/vip 映射（payment 和 membership 命名已统一为 basic/premium/enterprise/flagship）

### GitHub 已推送
- backend commit: `81c778c` → Render 自动部署中

### 架构说明（用户确认）
- **付款 = 独立事务**：注册/会员 与 付款 完全解耦
- **会员激活由 AI客服 单独调用** `/api/membership/check-and-activate`
- payment 回调只记录 `payment_orders`，不自动激活会员

### 待测试
- 扫码支付：基础版 ¥199（testMode 应为 false）
- 扫码支付：5000积分业务服务（¥50）
- 收钱吧回调 → payment_orders 写入

### 01:26 会员内容修改（截图最终版）

修改3个文件：membership.db.js / payment.db.js / MembershipPage.tsx

最终定价：basic ¥199 / premium(专业版) ¥499 / enterprise ¥1599 / flagship(旗舰版) ¥5888

Git commit c971ed6 已推送 Render 部署中

