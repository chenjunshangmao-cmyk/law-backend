## 2026-04-25 支付UI优化（09:07）

### 本次改动：去掉多余的"点击支付"按钮
- 收钱吧二维码本身已支持微信/支付宝，不需要额外的支付链接
- 去掉 ExternalLink 图标和"点击支付"按钮
- 更新文案：「用微信或支付宝扫码付款」+「扫码后自动识别支付方式」
- 已构建并部署到 Cloudflare Pages: https://1053af06.claw-app-2026.pages.dev

---

## 2026-04-25 支付链路修复（02:39）

### 本次修复：收钱吧回调3个堵点（付款完成关键）

**堵点1：terminalKey fallback 用错**
- 旧：fallback 到 vendorKey（677da...），与创建支付用的 terminalKey（96bfaf...）不一致 → 验签必失败
- 修复：fallback 到 HARDCODE_KEY（96bfaf...），与创建支付一致
- 文件：`src/routes/payment.db.js` webhook handler

**堵点2：result_code 第一层判断值**
- 旧：只接受 'SUCCESS'
- 修复：接受 'SUCCESS' 或 '200'（收钱吧可能返回两种）

**堵点3：biz_response.result_code 第二层判断值**
- 旧：只接受 'SUCCESS'
- 修复：接受 'SUCCESS' 或 'PAY_SUCCESS'（收钱吧文档示例为 'PAY_SUCCESS'）
- ⚠️ 注意：第二层失败后会 res.send('success') 悄悄返回，订单永远不会变 paid！

**提交：commit 704f382** → 已 push → 触发 Render 部署

### 上午修复（5项）

**修复1：终端配置兜底** `payment.db.js getActiveTerminal()`
- 硬编码配置（claw-web-new3）：terminalSn: 100111220054389553, terminalKey: 96bfaf401367d934cb10a1cbe9773647

**修复2：基础版测试价 ¥1.9** (basic: { price: 190 }) 正式收款改 19900

**修复3：业务服务积分换算** POINTS_PRICE_FEN=1 → 5000积分=¥50

**修复4：官方 WAP API** 替代硬编码 m.wosai.cn

**修复5：check-and-activate 兼容旧名** pro→premium, vip→flagship

### 关键新发现（00:56）

**testMode: true 的根本原因**：`payment.db.js` 里的 API 端点写错了！

| 文件 | 端点 | 状态 |
|------|------|------|
| `shouqianba.js`（SDK，正确） | `/upay/v2/wap2` | ✅ |
| `payment.db.js`（自定义实现，错误） | `/upay/v2/create` | ❌ |

同时响应字段 `payUrl` 应为 `pay_url`（收钱吧返回蛇形）。

### 已修复（本轮）
1. **API 端点**：`/upay/v2/create` → `/upay/v2/wap2`（与 SDK 一致）
2. **响应字段**：`payUrl` → `pay_url`（下划线）
3. **删除残留**：`upgradeUserMembership` 函数（会员激活由 AI客服 单独处理，不需要）
4. **membership.db.js 套餐价格同步**（单位 fen）：basic=190, premium=49900, enterprise=159900, flagship=588800
5. **简化 check-and-activate**：移除 pro/vip 映射（payment 和 membership 命名已统一为 basic/premium/enterprise/flagship）

### GitHub 已推送
- backend commit: `81c778c` → Render 自动部署中

### 架构说明（用户确认）
- **付款 = 独立事务**：注册/会员 与 付款 完全解耦
- **会员激活由 AI客服 单独调用** `/api/membership/check-and-activate`
- payment 回调只记录 `payment_orders`，不自动激活会员

### 待测试
- 扫码支付：基础版 ¥199（testMode 应为 false）
- 扫码支付：5000积分业务服务（¥50）
- 收钱吧回调 → payment_orders 写入

### 01:26 会员内容修改（截图最终版）

修改3个文件：membership.db.js / payment.db.js / MembershipPage.tsx

最终定价：basic ¥199 / premium(专业版) ¥499 / enterprise ¥1599 / flagship(旗舰版) ¥5888

Git commit c971ed6 已推送 Render 部署中

### 01:32 会员逻辑网站化（截图最终版）

**新增后端接口（membership.db.js）：**
- `GET /api/membership/admin/users` — 获取所有用户（含会员状态），需 admin/super_admin 权限
- `POST /api/membership/admin/activate` — 手动激活指定用户会员，需 admin/super_admin 权限
- `POST /api/membership/admin/deactivate` — 关闭会员降级为 free，需 admin/super_admin 权限

**新增前端（MembershipPage.tsx）：**
1. AI 激活流程说明区块（蓝底3步图解：扫码支付→联系客服→AI自动激活）
2. 管理员面板（role=admin/super_admin 可见）：用户列表+一键激活/关闭按钮
3. User类型新增 `super_admin` 角色和 `flagship` 套餐

**Git commit**: `ddafb56` → 前端已部署 Cloudflare Pages，后端 Render 部署中

### 02:18 支付成功后自动跳转首页

**修改**：`frontend/src/pages/MembershipPage.tsx`
- 收钱吧支付轮询成功（line 131-137）：`alert()` → `window.location.href = '/'`
- 普通支付轮询成功（line 158-163）：`alert()` → `window.location.href = '/'`

**部署**：
- 前端：https://2c5c7aee.claw-app-2026.pages.dev ✅


