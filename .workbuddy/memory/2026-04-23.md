# 2026-04-23 工作日志

## 会员套餐重构完成（5级定价系统）

### 完成内容
- 后端：PLANS 定义（5级：free/basic/pro/enterprise/flagship）+ Quota模型（ai_copy_used/ai_image_used/ai_video_used）+ dbService.getQuotaByUserId + updateQuota + quota/consume接口
- 前端：MembershipPage.tsx 完全重写（5套餐卡片+升级按钮）
- 权限锁定：SmartPublishPage（AI文案需basic+）+ AvatarPage（AI视频需basic+）+ DashboardPage（免费用户升级提示）
- 额度扣减：前端在调用AI前先POST /api/quota/consume检查额度

### 5级套餐定价
| 套餐 | 价格 | AI文案 | AI图片 | AI视频 | 代理国家 |
|------|------|--------|--------|--------|----------|
| 免费 | ¥0 | 0次 | 0次 | 0次 | 0国 |
| 基础版 | ¥199 | 50次/月 | 20次/月 | 1次/天 | 不可用 |
| 专业版 | ¥499 | 无限 | 100次/月 | 2次/天 | 1国 |
| 企业版 | ¥1599 | 无限 | 500次/月 | 10次/天 | 6国 |
| 旗舰版 | ¥5888 | 无限 | 无限 | 无限 | 12国+定制 |

### 部署状态
- 前端：https://5c563429.claw-app-2026.pages.dev（2026-04-23 10:21）
- 后端：Render（GitHub: law-backend，commit 2d0dcdc，自动部署中）

### ServicesPage 升级（2026-04-23 10:39 完成）
- 顶部 Banner：深色渐变背景 + 收款方式图标栏（微信💚 / 支付宝💙 / 扫码支付）
- 卡片样式：渐变背景 + 彩色边框 + 推荐徽章 + 价格大字显示
- 弹窗：收款方式按钮栏 + 微信/支付宝图标
- 底部收款说明 + 服务流程3步骤
- 部署：https://e5549ccf.claw-app-2026.pages.dev

## 支付链路核心修复（2026-04-23 19:03）

### 根本原因
`payment.db.js` 和 `shouqianba.db.js` 使用了**不同的终端文件路径**：
- `shouqianba.db.js` 写入：`/app/data/shouqianba-terminal.json` ✅（activate时）
- `payment.db.js` 读取：`/app/src/data/shouqianba-terminal.json` ❌（空文件）
- 结果：payment找不到终端 → testMode=true → 没有真实payUrl

同时，`createWapPayment`（service层）使用了错误的收钱吧端点（`/upay/v2/wap2` POST），而正确工作的是WAP GET网关（`https://m.wosai.cn/qr/gateway`）。

### 修复内容
1. `payment.db.js`：`TERMINAL_FILE` 路径改为 `__dirname + '../../data/shouqianba-terminal.json'`（与shouqianba.db.js完全一致）
2. `payment.db.js`：移除 `createWapPayment` 调用，改为直接生成WAP URL（GET网关，参数带签名）
3. `payment.db.js`：添加 `import crypto from 'crypto'`
4. 移除未使用的 import：`createWapPayment`, `queryOrder`, `handleNotify`

### 验证结果
```
create-order: 200 REAL HAS_URL ✅  ← 真实支付URL生成（testMode: false）
status: 200 {status:"pending"} ✅   ← 订单状态查询正常
```
- 后端：Render 已部署（commit 61ac3ee）
- 测试用户：admin@claw.com
- 终端：claw-web-new3（SN: 100111220054389553）

### 完整支付流程
用户点击"购买" → 前端 POST /api/payment/create → 后端读取终端JSON → 生成WAP URL → 返回payUrl → 前端显示二维码 → 用户扫码支付 → 收钱吧回调 → 后端更新status=paid → 前端轮询确认 → 弹窗提示成功

### AuthContext 登录 Bug（2026-04-23 19:40）
**问题**：用户看到"用户不存在或会话已失效，请重新登录"（401错误）\n**根本原因**：后端返回 `{ data: { user, token } }`，但 AuthContext.tsx 从 `res`（不是 `res.data`）解构 token 和 user，导致 `newToken=undefined`，localStorage 存的 token 永远是 undefined。登录表面上"成功"，但 token 实际从未保存。\n**修复**：AuthContext.tsx 的 `login()` 和 `register()` 中，改 `const { token, user } = res;` 为 `const { token, user } = res.data || {};`\n**部署**：https://5b833be3.claw-app-2026.pages.dev ✅

## 会员状态不同步 Bug 修复（2026-04-23 20:49）

### 症状
支付成功后：
- ✅ 后端日志显示 `✅ 用户升级到 basic`
- ✅ 订单状态变为 `paid`
- ✅ 前端弹出"支付成功！会员已激活"
- ❌ 账号页面仍显示"免费版"

### 根本原因
`/api/auth/profile` 用 `findUserById()` 读**本地 JSON 缓存**（`data/users.json`），而不是 PostgreSQL 数据库。`upgradeUserMembership` 更新了数据库，但 profile 接口永远返回缓存里的旧数据。

### 修复
`backend/src/routes/auth.min.js` 的 `/api/auth/profile` 改为优先查 PostgreSQL：
```js
const pgResult = await pool.query(
  `SELECT id, email, name, membership_type, membership_expires_at
   FROM users WHERE email = $1`,
  [decoded.email]
);
return res.json({ success: true, data: { user: {
  plan: dbUser.membership_type || 'free',
  membershipType: dbUser.membership_type || 'free',
  ...
}}});
```
同时 `upgradeUserMembership` SQL 改用 `id::text = $3`（与其他地方一致）。

### 部署
- 后端 Render：`b846fbc` ✅
- 收钱吧终端：`claw-web-new3` SN: `100111220054389553` 在线 ✅

## YouTube OAuth 修复（2026-04-23 17:54）

### 问题
Google OAuth 报错 `400: redirect_uri_mismatch`

### 原因
后端生成的 `redirect_uri` 包含查询参数 `?mode=popup`，但 Google Cloud Console 里配置的 URI 不包含参数，导致不匹配。

### 修复
- 修改 `auth.youtube.js`：
  - 移除 `redirect_uri` 中的 `mode` 查询参数
  - 将 `mode` 信息通过 `state` 参数传递
  - 回调时从 `state` 中解析 `mode`

### 提交
- Commit: `e178d01`
- 已推送到 GitHub，Render 自动部署中

### 待验证
- [ ] Render 部署完成
- [ ] YouTube 授权正常弹出
- [ ] TikTok 登录保存正常

## 收钱吧回调三层状态修复（2026-04-23 19:57）

### 来源
桌面文件：收钱吧C扫B-PRO支付验收文档V1.0.3.xlsx

### 修复内容
按验收标准"机制验收-回调验签机制"补全三层状态判断：

**第一层（通讯层）**：`result_code === 'SUCCESS'`
**第二层（业务结果层）**：`biz_response.result_code === 'SUCCESS'`
**第三层（订单状态层）**：`order_status === 'PAID'`

同时：
- 防止重复处理（已有paid状态跳过）
- CLOSED/REFUND 状态也记录到数据库
- 失败统一返回 `'success'` 避免收钱吧无限重试

### 涉及文件
- `src/routes/payment.db.js`（本地）
- `src/routes/shouqianba.db.js`（本地）
- `backend/src/routes/payment.db.js`（Render后端，已推送）

### 部署
- 后端 Render：已推送 commit `a7dd3d3` ✅

## 登录/注册页面 Bug（ModernLogin + ModernRegister）修复（2026-04-23 22:24）

### 根本原因
App.tsx 导入的是 `ModernLogin.tsx` 和 `ModernRegister.tsx`，不是旧的 `LoginPage.tsx`。

**ModernLogin.tsx 错误写法**：
```js
const response = await api.auth.login(email, password); // publicFetch 已解包 → { user, token }
if (response.success) {   // ❌ 永远 undefined（已解包，无 success 字段）
  await login(response.data.token, response.data.user); // ❌ 传错参数
```

**正确写法（与 LoginPage.tsx 一致）**：
```js
await login(email, password); // ✅ 直接用 AuthContext 的 login
navigate('/dashboard');
```

### 修复内容
1. **ModernLogin.tsx**：改用 `login(email, password)`，不用 API 直接调用
2. **ModernRegister.tsx**：改用 `register(form.email, ...)`，不用 API 直接调用
3. **HomePage.tsx**：版本号 `v1.1.0422` → `v1.1.0423`

### 部署
- 前端：https://9e8f64a8.claw-app-2026.pages.dev ✅
- 后端：已是最新（1.1.0423b）

## 收钱吧今日重大修复（2026-04-23 23:19-23:24）

### Bug1：webhook验签用错密钥（commit da07847）
- **问题**：payment.db.js 回调验签用 vendorKey（机构密钥），但WAP支付创建时用 terminalKey 签名 → 验签永远失败 → 真实支付无法激活会员
- **修复**：从数据库 shouqianba_terminals 表按 terminal_sn 查 terminalKey 再验签
- **文件**：backend/src/routes/payment.db.js
- **commit**：de9ae38 → da07847（已推送 GitHub → Render 部署中）

### Bug2：实际付款金额少100倍（commit da07847）
- **问题**：shouqianba.db.js /create-order 没有把元转成分，前端传199元 → 后端发199分给收钱吧 → 用户只付¥1.99
- **修复**：`total_amount: String(Math.round(Number(totalAmount) * 100))`
- **commit**：da07847（已推送 GitHub → Render 部署中）

### 重要教训
- 收钱吧金额单位永远是**分**，API传参前必须×100
- 验签密钥：WAP用terminalKey（不是vendorKey）
- 本次修复后收钱吧可正式收款

## 业务服务支付修复（2026-04-23 23:31）

### 问题
- ServicesPage: 前端传 `amount: 5000`（元），但后端 SERVICES 已定义分（500000），导致金额错误
- 没有收钱吧直连支付（微信/支付宝弹窗）

### 修复
1. **后端**：SERVICES 改为元（5000）+ `orderAmount = serviceInfo.price * 100` 转分
2. **前端 ServicesPage**：完全重构，与 MembershipPage 风格一致
   - 增加「💚 微信/支付宝」按钮 + 收钱吧弹窗（二维码+轮询）
   - 卡片布局改为3列grid，带热门标签
   - api.shouqianba.createOrder 新增可选 `subject` 参数
3. **部署**：https://45ff22f1.claw-app-2026.pages.dev ✅
4. **后端**：commit e003099（已推送 GitHub → Render 部署中）
